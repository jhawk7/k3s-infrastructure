apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mqtt-sub-client
  namespace: mqtt
spec:
  selector:
    matchLabels:
      app: mqtt-sub-client
  template:
    metadata:
      labels:
        app: mqtt-sub-client
    spec:
      nodeSelector:
        type: agent
        kubernetes.io/arch: arm64
      imagePullSecrets:
      - name: mqtt-registry-credentials
      containers:
      - name: redis
        image: redis:alpine
        command: ["/bin/sh", "-c"]
        args:
          - redis-server --requirepass "$${REDIS_PASS:?REDIS_PASS not set}" --save 60 1
        envFrom:
        - secretRef:
            name: mqtt-sub-client-redis-env
        volumeMounts:
        - name: mqtt-sub-client-redis-pvc
          mountPath: /data
        ports:
        - containerPort: 6379
      - name: mqtt-sub-client
        image: jhawk7/mqtt-sub-client@sha256:5f2585f46713f8b6501a20c16c5c02c972c04972fa813ae922f6f231f8987eee
        imagePullPolicy: IfNotPresent
        envFrom:
        - secretRef:
            name: mqtt-sub-client-secret
        - secretRef:
            name: mqtt-sub-client-redis-env
  volumeClaimTemplates:
  - metadata:
      name: mqtt-sub-client-redis-pvc
      namespace: mqtt
    spec:
      accessModes: [ "ReadWriteOnce" ]
      storageClassName: "nfs"
      resources:
        requests:
          storage: 100Mi
